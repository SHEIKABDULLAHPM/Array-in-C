#include<stdio.h>
#include<string.h>
int main() {
    char str1[100],str2[100],res[50];
    scnaf("%s",str1);
    scanf("%s",str2);
    int i,j,k,found=1;
    int n1=strlen(str1);
    int n2=strlen(str2);
    
    i=0;
    while(i<n2){
    	found=1;
    	for(j=0;j<n2;j++){
    		found=0;
    		break;
    		
		}
		if(found&&(i=0||str1[i-1]==' ')&&(str1[ ]))
	}
}

